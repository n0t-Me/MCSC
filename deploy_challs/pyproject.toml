[tool.poetry]
name = "deploy-challs"
version = "0.1.0"
description = ""
authors = ["git <git@local>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
ctfcli = "^0.1.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
